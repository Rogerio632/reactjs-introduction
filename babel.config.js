module.exports = {
  presets: [
    "@babel/preset-env", //responsável por alteras as propriedades JS que o
                        // browser ainda não entende.
    "@babel/preset-react" // responsável por alterar as propriedades do React
                          // para que seja legível para o browser.
  ]

};